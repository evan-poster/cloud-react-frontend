import React from 'react';

function App() {
	const message = "Hello from Group";
	return (
		<div style={{textAlign: 'center', marginTop: '20%'}}>
			<h1>{message}</h1>
		</div>
	);
}

export default App;
